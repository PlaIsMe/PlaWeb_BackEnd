server:
  port: 8080
  servlet:
    context-path: /

spring:
  datasource:
    url: 'jdbc:mysql://localhost:3306/plaweb'
    driverClassName: "com.mysql.cj.jdbc.Driver"
    username: root
    password: rootroot
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 25MB
      max-request-size: 25MB